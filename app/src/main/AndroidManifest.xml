<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.developer.projetounivesp2021_frontrnd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoUnivesp2021Frontrnd">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".SimpleSelectListActivity" />
        <activity android:name=".FragmentActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.ProjetoUnivesp2021Frontrnd.Fullscreen" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.ProjetoUnivesp2021Frontrnd.Fullscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>